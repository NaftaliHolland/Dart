/**
 * Trying to make use of some of the available string functions available in dart
 */

void main() {
  String test = 'We all know that cinsistency is key';

  var tt = test.length;
  print(tt);
}
